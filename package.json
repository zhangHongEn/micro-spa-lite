{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
    "scripts": {
      "dev": "pnpm --parallel --filter './*' dev",
      "preview": "pnpm --parallel --filter './*' preview",
      "build": "pnpm --parallel --filter './*' build"
    },
    "engines": {
  "pnpm": ">=7 <10"
},
  "devDependencies": {
  }
}
